# CareFlow 开发约束

本仓库以可持续维护的开源项目为目标。执行任何修改前阅读本文、README.md 和相关模块文档。

## 架构与安全

- 保持独立知识库底座与应用层边界，业务意图配置属于应用层。
- Java 拥有身份、授权、元数据、任务、发布和计量；Python 承担解析、模型与检索适配；Web 只调用公共 Java API。
- 新业务规则进入服务层；控制器负责输入校验和协议映射。现有控制器耦合按 docs/quality.md 逐步拆分，禁止继续堆叠。
- 不信任模型输出；模型不授予权限、不执行业务动作。每个敏感数据边界重新校验租户和权限。
- 禁止提交真实凭证、个人数据、环境文件或含正文的调试日志；外部请求必须与用户授权任务相关。
- 模型或依赖不可用时显式失败；禁止用伪造结果掩盖运行失败。
- 数据迁移新增版本，不修改已应用的迁移。破坏性变更必须说明迁移与恢复方式。

## 交付要求

- 一次修改解决一个明确问题；先理解现有实现，不引入无关依赖、重复逻辑或大规模无验证重写。
- 新功能附行为测试；缺陷修复附能复现问题的回归测试。格式或文档修改无需镜像测试。
- 按 CONTRIBUTING.md 执行对应检查；报告实际执行结果、跳过项与限制。
- 接口、配置、运行方式变化必须同步文档；架构决策进入 docs/adr/。
- 禁止将单元测试、页面存在或局部模型调用表述为完整验收通过。
- 提交前检查模块边界、安全、可读性、可维护性和文档一致性。未通过检查不得宣称可发布。
