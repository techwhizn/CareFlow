**可扩展企业知识库平台 PRD**

版本：V1.0
定位：商业级知识库与检索服务平台
首期目标：完成可真实运行、可评估、可对外提供服务的知识库闭环
文档用途：Codex 开发输入、需求范围基线、验收依据

**1．产品目标与核心决策**

建设一个支持企业资料管理、可视化知识切片、Milvus 向量存储、Hybrid Search、Rerank、引用问答及 API 调用的平台。

平台未来能够接入不同业务应用，但知识库不承担具体业务意图识别和业务执行职责。

必须遵守以下决策：

1. 知识库作为独立能力，可以单独提供检索和问答服务。
2. 文档解析、切片、索引、检索、重排、问答具有清晰边界。
3. 不同业务应用拥有自己的意图配置、示例和业务路由。
4. 应用可以复用授权知识库，但不能通过应用配置扩大用户权限。
5. 首期采用模块化架构，不提前拆成大量微服务。
6. 扩展能力通过接口、配置版本和适配器实现，首期不建设插件市场。
7. 先完成真实业务闭环，再扩展高级能力。
8. Milvus、Embedding、Rerank 必须真实接入，不能用模拟结果冒充完成。

**2．目标用户与使用场景**

| 用户     | 主要需求                     |
| ------ | ------------------------ |
| 企业管理员  | 管理成员、权限、模型配置、套餐和额度       |
| 知识管理员  | 上传资料、检查解析结果、调整切片、发布和更新知识 |
| 普通使用者  | 搜索资料、提问、核对引用、反馈错误        |
| 应用开发者  | 创建应用、绑定知识库、调用检索和问答 API   |
| 质量运营人员 | 维护测试集、分析错误、补全知识          |
| 平台运维人员 | 监控任务、定位故障、管理容量和恢复数据      |

代表性使用场景：

* 上传产品手册，修正解析错误并发布，用户能够搜索和问答。
* 同义表达可以找到对应知识，型号、错误码等关键标识得到保留。
* 新版制度先测试再发布，支持回滚。
* 同一知识库被客服助手和技术支持助手分别使用。
* 文档删除或撤权后，检索、问答、引用和历史记录同步限制访问。
* 根据用户差评定位切片、召回、重排或生成阶段的问题。

**3．首期范围与后续规划**

优先级定义：

* **P0：首期必须交付。**
* **P1：试点使用后的增强能力。**
* **P2：后续扩展方向，首期只预留边界。**

| 模块    | P0                                 | 后续能力                |
| ----- | ---------------------------------- | ------------------- |
| 企业与权限 | 企业、成员、固定角色、知识库和文档权限                | 部门、用户组、SSO、自定义角色    |
| 知识库   | 创建、配置、授权、归档、删除                     | 模板、迁移、跨环境复制         |
| 文档接入  | PDF、DOCX、MD、TXT、CSV/XLSX、图片 OCR    | 网页同步、企业文档连接器、PPTX   |
| 文档解析  | 正文、标题、段落、基础表格、页码、OCR               | 复杂图文、公式、跨页复杂表格      |
| 切片    | 结构、递归、父子、FAQ、基础表格、人工编辑             | 语义切片、自动策略推荐         |
| 检索    | Milvus、向量与 BM25 混合召回、RRF、模型 Rerank | 多查询扩展、复杂检索优化        |
| 问答    | 搜索、流式问答、引用、拒答、基础多轮、反馈              | 长文研究、多模态问答          |
| 应用    | 创建应用、绑定知识库、配置、API Key              | 意图识别、业务路由、工作流       |
| 评估    | 测试集、检索指标、人工回答评审、版本比较               | 自动评审、在线实验           |
| 商业化   | 套餐、人工开通、用量、配额、额度调整                 | 在线支付、自动续费、发票对接      |
| 运维    | 日志、监控、任务恢复、备份验证                    | 专属资源、完整私有化交付、正式 SLA |

首期明确不包含：

* 退款、订单、请假等业务意图配置。
* 业务工具执行和自主 Agent。
* 知识图谱。
* 模型训练平台。
* 动态插件市场。
* 在线支付。
* 任意用户脚本执行。

**4．架构边界与扩展原则**

| 层级    | 职责                   | 不承担的职责    |
| ----- | -------------------- | --------- |
| 平台基础层 | 企业、身份、权限、密钥、计量、审计、任务 | 具体业务理解    |
| 知识能力层 | 文档、切片、索引、检索、重排、引用问答  | 业务意图与业务操作 |
| 应用层   | 应用配置、资源绑定，后续意图识别和路由  | 修改知识库底层职责 |
| 业务集成层 | 后续订单、工单、审批等系统对接      | 替代知识检索    |

依赖方向：

**应用调用知识能力，知识能力不反向依赖具体业务应用。**

首期保留以下扩展接口：

| 扩展接口          | 输入与输出                   |
| ------------- | ----------------------- |
| 文档解析器         | 文件与解析配置 → 带来源位置的结构化内容块  |
| 切片策略          | 内容块与策略配置 → 切片、父子关系、定位信息 |
| Embedding 适配器 | 文本批次 → 向量、模型身份、用量       |
| 检索后端          | 查询与授权过滤 → 候选片段          |
| Rerank 适配器    | 查询与候选片段 → 排名与分数         |
| 生成模型适配器       | 问题与证据 → 答案、引用、用量        |
| 数据源连接器        | 内容、版本、删除和权限变更 → 导入事件    |
| 意图识别接口        | 应用配置与对话 → 意图候选、实体、待补信息  |

意图识别接口首期只定义契约，不实现识别引擎和意图库页面。

扩展接口必须明确版本、超时、错误类型、用量记录和能力声明。首期采用开发者注册的适配器，不允许用户上传代码直接运行。

**5．默认技术方案**

以下是项目默认方案，开发开始时需检查现有仓库并锁定兼容版本。

| 部分        | 默认方案               | 职责                    |
| --------- | ------------------ | --------------------- |
| 前端        | React + TypeScript | 管理后台、搜索、问答            |
| 主后端       | Java + Spring Boot | 权限、业务状态、API、计量        |
| 文档与 AI 任务 | Python Worker      | 解析、OCR、切片、模型处理        |
| 业务数据库     | MySQL              | 权威元数据、版本、任务、权限、账务     |
| 检索数据库     | Milvus             | 向量与 BM25 检索           |
| 对象存储      | S3 兼容存储            | 原始文件、解析产物             |
| 消息队列      | RabbitMQ           | 异步任务投递                |
| 缓存        | Redis              | 限流与必要缓存               |
| 模型接入      | 可替换适配器             | Embedding、Rerank、生成模型 |

约束：

* Java 后端采用模块化单体，Worker 独立运行。
* 首期不引入完整微服务治理体系。
* 不为 BM25 额外引入 Elasticsearch，除非实际需求证明 Milvus 无法满足。
* 不预先锁定模型供应商，但必须至少接通各类模型的一个真实实现。
* 密钥由受控配置管理，不能写入前端、日志或代码仓库。
* 开发环境可以使用轻量部署；不能据此宣称生产高可用。

Milvus 官方提供 BM25 全文检索和多向量混合检索能力；实施时需要按照最终锁定版本核验 API、中文分析器及索引支持。[全文检索文档](https://milvus.io/docs/full-text-search.md)、[混合检索文档](https://milvus.io/docs/multi-vector-search.md)

**6．企业、身份与权限**

| 编号      | 需求                                |
| ------- | --------------------------------- |
| AUTH-01 | 支持企业、成员、成员禁用和移除                   |
| AUTH-02 | 首期提供企业所有者、企业管理员、知识管理员、应用开发者、使用者角色 |
| AUTH-03 | 权限动作至少区分读取、下载、编辑、发布、管理            |
| AUTH-04 | 知识库具有 ACL，文档默认继承，可进一步收窄权限         |
| AUTH-05 | 应用绑定知识库需验证授权，不能跨租户绑定              |
| AUTH-06 | API Key 具有应用身份、作用范围、失效时间、撤销与轮换能力  |
| AUTH-07 | 搜索、问答、引用、原文下载、任务详情和历史记录统一鉴权       |
| AUTH-08 | 平台运维默认只读取运行元数据，无权直接阅读企业资料         |

有效访问范围为：

**企业范围 ∩ 调用身份权限 ∩ 应用绑定范围 ∩ 文档权限 ∩ 当前有效发布范围。**

具体规则：

* 企业身份从可信认证上下文获取，不接受请求正文覆盖。
* 客户端过滤条件只能缩小检索范围。
* 两路召回都应用权限过滤。
* 候选送往外部 Rerank 或生成模型前，再次检查当前权限。
* 父片段和相邻片段扩展不得绕过权限。
* 权限服务异常时拒绝请求，不能自动放行。
* 服务端 API Key 代表应用身份，不能把客户端传入的用户 ID 当作已验证身份。
* 外部应用需要用户级权限时，应使用可验证的身份委托；未实现前必须明确能力限制。

撤权提交后，新请求必须立即按新权限处理。执行中的请求在继续输出前检查授权变化并中止；已交付给用户的内容无法追回。

历史答案保存证据依赖。任一依赖被删除或撤权后，首期隐藏对应整条答案及引用，避免只隐藏引用却保留敏感正文。

**7．知识库管理**

| 编号    | 功能     | 验收要求                |
| ----- | ------ | ------------------- |
| KB-01 | 创建知识库  | 名称、描述、语言、标签、责任人     |
| KB-02 | 配置管理   | 默认解析、切片、模型和检索配置     |
| KB-03 | 配置版本   | 草稿修改不影响线上配置         |
| KB-04 | 授权管理   | 成员、权限动作、应用引用        |
| KB-05 | 状态概览   | 文档数、有效切片数、失败任务、空间占用 |
| KB-06 | 归档与恢复  | 归档停止新检索，恢复需重新检查有效状态 |
| KB-07 | 删除     | 提示文档、应用和会话影响范围      |
| KB-08 | 应用引用关系 | 展示哪些应用正在使用知识库       |

知识库不能出现“退款意图”“人事路由”等业务配置字段。

**8．文档接入与生命周期**

| 编号     | 功能    | 具体要求                         |
| ------ | ----- | ---------------------------- |
| DOC-01 | 批量上传  | 默认单文件不超过 50 MiB，单批不超过 20 个   |
| DOC-02 | 格式验证  | 检查实际文件类型，拒绝损坏、加密和不支持格式       |
| DOC-03 | 重复检测  | 同知识库按内容摘要提示重复，不静默覆盖          |
| DOC-04 | 文档元数据 | 标题、来源、标签、产品型号、语言、生效与失效时间     |
| DOC-05 | 结构解析  | 提取正文、标题路径、段落、列表、基础表格         |
| DOC-06 | OCR   | 支持扫描 PDF、PNG/JPEG，展示识别质量警告   |
| DOC-07 | 清洗    | 页眉页脚、断行、空白处理，保留前后对照          |
| DOC-08 | 来源定位  | PDF 页码；DOCX 段落；表格工作表和行列；文本偏移 |
| DOC-09 | 版本更新  | 替换文件生成新版本，不直接覆盖已发布版本         |
| DOC-10 | 错误恢复  | 展示失败阶段、原因、重试和重新上传入口          |

默认 PDF 页数上限为 500 页，可通过套餐收紧。大型文件的限制应在上传或解析早期明确返回。

无法恢复的复杂表格必须标记警告，不把严重乱码内容当作成功解析。

处理状态与发布状态独立：

| 类型   | 状态                       |
| ---- | ------------------------ |
| 处理状态 | 排队、解析、切片、向量化、索引、就绪、失败、取消 |
| 发布状态 | 草稿、已发布、归档、已删除            |

“处理就绪”不等于“已经发布”。只有已发布且处于有效期内的内容参与默认检索。

任务可靠性要求：

* 数据库保存任务权威状态，消息队列负责投递。
* 使用 Outbox 或等价机制保证业务变更和任务投递可恢复。
* Worker 支持幂等键、心跳、阶段检查点和取消标记。
* 消息重复消费不得生成重复有效切片或重复扣费。
* 默认最多自动尝试 3 次；损坏文件等不可恢复异常不反复重试。
* 过期任务不能覆盖新版本，迟到回调不能使已删除文档重新出现。
* 对每个租户设置任务并发和公平调度。
* 无法估算进度时显示阶段，不伪造百分比。

**9．可视化切片工作台**

切片工作台是首期核心页面，必须提供“原文—切片—检索效果”的关联。

| 编号    | 功能      | 规则                       |
| ----- | ------- | ------------------------ |
| CH-01 | 结构与递归切片 | 优先按标题、段落、句子拆分，超限继续细分     |
| CH-02 | 父子切片    | 子片段用于匹配，父片段用于上下文补全       |
| CH-03 | FAQ 切片  | 问题、相似问法和答案保持关联           |
| CH-04 | 表格切片    | 保留表名、列名、单位和行关联           |
| CH-05 | 固定长度兜底  | 明确采用 Token 计量            |
| CH-06 | 双栏对照    | 点击切片定位原文                 |
| CH-07 | 发布前预览   | 数量、长度分布、警告和预计处理用量        |
| CH-08 | 人工修改    | 编辑、拆分、合并、禁用、批量标签         |
| CH-09 | 质量检查    | 空内容、过短、超长、重复、OCR 异常、表格断裂 |
| CH-10 | 修订追踪    | 记录修改人、时间、原因、来源版本         |
| CH-11 | 增量处理    | 只重新处理发生变化的检索内容           |
| CH-12 | 冲突处理    | 原文更新后，提示人工修订与新解析结果的冲突    |

建议起始参数：

| 参数      |         默认值 |
| ------- | ----------: |
| 子切片目标长度 |   400 Token |
| 子切片上限   |   600 Token |
| 重叠长度    |    60 Token |
| 父片段上限   | 1,600 Token |

这些是待评估的初始配置，不是所有资料的最佳值。实际输入必须同时满足模型限制，附加标题和表头也计入预算。

数据规则：

* 保留原始来源文本、实际检索文本和修订记录。
* 已发布切片不可直接修改，修改先形成草稿。
* 人工修改不改写原始文件。
* 无法对应原文的人工新增内容，标记为人工知识补充，不伪造引用。
* 并发修改通过版本检查返回冲突，不能静默覆盖。
* 新版文档不得悄悄覆盖旧版人工修订。

**10．Milvus 索引与知识发布**

| 编号     | 功能          | 规则                      |
| ------ | ----------- | ----------------------- |
| IDX-01 | 模型身份管理      | 记录供应商、模型版本、维度、配置摘要和距离度量 |
| IDX-02 | 稠密与 BM25 索引 | 指向同一切片版本，保留必需过滤字段       |
| IDX-03 | 增量更新        | 按内容和配置变化处理              |
| IDX-04 | 状态管理        | 待处理、处理中、可检索、失败          |
| IDX-05 | 一致性检查       | 对比业务文档、有效切片和检索记录        |
| IDX-06 | 索引重建        | 新代际构建验证后切换              |
| IDX-07 | 发布与回滚       | 发布指针可追踪，回滚不恢复已撤销权限      |
| IDX-08 | 删除清理        | 先阻止访问，再异步清理所有副本         |

必须明确：

* 文档和查询必须使用兼容的向量模型。
* 同维度不代表不同模型的向量可以混用。
* 更换 Embedding 模型通常需要重建向量索引。
* Milvus 不是文档版本、权限和账务的权威数据库。
* 插入 Milvus 成功不等于对用户发布成功。

首期可以按租户和模型配置组织 Collection，通过索引路由表隐藏物理结构。所有检索仍需服务端强制过滤。后续允许替换为共享或专属资源策略，不影响公共 API。

Milvus 提供多种租户组织方式，具体容量与隔离效果仍需按部署环境验证。[Milvus 多租户文档](https://milvus.io/docs/multi_tenancy.md)

发布流程：

1. 新版本解析、切片和索引进入暂存状态。
2. 检查记录完整性和实际搜索可见性。
3. 在业务事务中切换发布清单，增加知识发布版本。
4. 一次请求固定内容发布快照，避免同一文档新旧版混用。
5. 权限始终按最新撤权状态检查。
6. 重建失败不影响当前可用版本。
7. 已删除对象的迟到任务不得恢复其可见性。

不能假设 MySQL 与 Milvus 之间存在跨库原子事务，必须设计补偿和核对机制。

**11．Hybrid Search 与 Rerank**

标准流程：

**认证与范围确定 → 查询处理 → 两路召回 → 融合 → 权限复核 → Rerank → 上下文整理 → 返回证据。**

| 编号     | 功能        | 默认规则                  |
| ------ | --------- | --------------------- |
| RET-01 | 检索模式      | 语义、关键词、混合；默认混合        |
| RET-02 | 语义召回      | 查找意思相关的内容             |
| RET-03 | BM25 召回   | 补充术语、型号、错误码匹配         |
| RET-04 | 元数据过滤     | 类型化字段和操作符白名单          |
| RET-05 | 结果融合      | 首期使用 RRF              |
| RET-06 | 模型 Rerank | 对问题与候选片段重新评分排序        |
| RET-07 | 去重        | 避免重叠切片和同源内容占满结果       |
| RET-08 | 上下文扩展     | 补充同版本父片段或相邻片段         |
| RET-09 | 相关性门槛     | 通过测试集标定               |
| RET-10 | 降级        | 按应用配置决定降级或返回错误        |
| RET-11 | 查询处理      | 保留原始问题与改写，不能丢失型号等关键实体 |
| RET-12 | 调试信息      | 分阶段候选、排名、分数、耗时和配置版本   |

起始参数：

| 参数             |         默认值 |
| -------------- | ----------: |
| 每路召回数量         |          40 |
| 单路最大数量         |         200 |
| RRF 参数 k       |          60 |
| 进入 Rerank 的候选数 |       最多 40 |
| 最终证据片段数        |        最多 6 |
| 同一文档片段数        |        最多 3 |
| 问答证据预算         | 6,000 Token |

参数受模型上下文和套餐资源限制，必须允许配置和评估。

融合与模型重排是不同阶段，不能只使用 RRF 就宣称完成模型 Rerank。[Milvus 重排文档](https://milvus.io/docs/reranking.md)

降级规则：

* Rerank 超时：应用允许时返回融合结果，标记 `degraded`。
* Embedding 不可用：应用允许时退化为 BM25。
* 权限检查失败：拒绝请求。
* 完全没有证据：返回无证据结果，不伪装成系统成功回答。
* 系统故障和“资料中没有答案”必须区分。

不把向量分数、BM25 分数、Rerank 分数显示成答案正确率。

**12．搜索、问答与引用**

| 编号    | 功能   | 验收要求                     |
| ----- | ---- | ------------------------ |
| QA-01 | 独立搜索 | 不调用生成模型也能查询片段            |
| QA-02 | 流式问答 | 展示处理状态、答案增量、引用和完成状态      |
| QA-03 | 停止生成 | 取消后停止继续交付，记录实际用量         |
| QA-04 | 基础多轮 | 默认最近 6 轮且不超过 3,000 Token |
| QA-05 | 证据引用 | 绑定文档版本、切片版本、来源位置         |
| QA-06 | 原文查看 | 点击引用定位证据，下载单独授权          |
| QA-07 | 证据不足 | 说明无法确认，必要时澄清             |
| QA-08 | 冲突处理 | 展示不同来源及适用时间              |
| QA-09 | 用户反馈 | 点赞、点踩、错误原因、补充说明          |
| QA-10 | 会话历史 | 用户与应用隔离，重新打开时校验权限        |

问答限制：

* 默认只根据授权知识回答业务事实。
* 只能引用本次检索证据中的合法 ID。
* 引用合法不等于语义正确，仍需评估是否支持结论。
* 不允许文档中的指令改变系统权限或规则。
* 首期不执行业务工具。
* 生成失败的部分答案必须标记未完成。
* 不记录模型隐含推理过程。
* 切换应用开启新会话，避免业务上下文混用。

**13．应用中心与未来意图识别**

首期应用中心必须保持轻量。

| 编号     | P0 功能                |
| ------ | -------------------- |
| APP-01 | 创建应用，配置名称、描述、负责人     |
| APP-02 | 绑定一个或多个同租户知识库        |
| APP-03 | 绑定检索策略、模型配置和问答约束     |
| APP-04 | 应用配置草稿、发布、回滚         |
| APP-05 | 应用级 API Key、限流、用量与日志 |
| APP-06 | 通过应用调用搜索和问答          |

首期应用类型只启用“知识问答”。不制作不可用的意图页面或工作流页面。

未来意图识别遵守：

* 配置归属于 `tenant_id + application_id`。
* 每个应用拥有自己的意图目录、正反例、参数、澄清规则和发布版本。
* 同名意图不能自动跨应用复用。
* 识别引擎可以共享，业务配置不能混用。
* 知识库不知道调用方属于退款、人事还是技术支持业务。
* 意图识别结果只是建议，权限与业务规则决定是否允许执行。

未来接口的业务契约：

| 输入                       | 输出                                |
| ------------------------ | --------------------------------- |
| 应用身份、意图配置版本、用户文本、受限对话上下文 | 意图候选、实体、缺失参数、是否需要澄清、配置版本、Trace ID |

路由配置作为独立环节，将识别结果映射到知识检索、业务查询、业务流程或人工入口。识别引擎本身不执行退款等操作。

允许一个请求包含多个意图，不能把底层数据结构限制为永远只有一个意图。

**14．检索调试与质量评估**

调试台必须能够解释“为什么没答对”。

| 编号      | 功能                        |
| ------- | ------------------------- |
| EVAL-01 | 展示原问题、改写结果、实际检索范围         |
| EVAL-02 | 对照语义与 BM25 候选             |
| EVAL-03 | 对照融合前后和 Rerank 前后排名       |
| EVAL-04 | 展示最终证据及被排除原因              |
| EVAL-05 | 展示分阶段耗时、用量与模型版本           |
| EVAL-06 | 建立测试集，记录问题、参考答案、正确证据和测试身份 |
| EVAL-07 | 对比不同切片、模型、检索配置版本          |
| EVAL-08 | 将差评与无结果查询转为知识改进任务         |

首期测试集建议至少 120 题：

| 类型            | 建议数量 |
| ------------- | ---: |
| 可直接回答的业务问题    |   50 |
| 同义表达与口语问题     |   20 |
| 型号、错误码、编号问题   |   20 |
| 文档没有答案的问题     |   15 |
| 版本、有效期或资料冲突问题 |   15 |

权限隔离另设自动化验收用例，不计入普通问答准确率。

评估指标：

* Recall@K：相关证据被召回的比例。
* Hit@K：前 K 条至少包含一条正确证据的问题比例。
* MRR：首个正确证据的排名表现。
* 回答正确性、证据支持程度、引用准确性。
* 无答案拒答率与有答案误拒答率。
* P50/P95 延迟、失败率、单次成本。

自动评审属于后续增强。首期采用人工标注和确定性检索指标，不把模型自评作为唯一验收依据。

**15．页面与交互清单**

| 页面    | 必需内容              | 主要操作         |
| ----- | ----------------- | ------------ |
| 工作台   | 知识、任务、用量、异常概览     | 进入失败任务和待发布内容 |
| 知识库列表 | 名称、状态、规模、责任人      | 创建、搜索、归档     |
| 知识库详情 | 文档、配置、权限、引用应用     | 上传、配置、授权     |
| 文档详情  | 原文、解析结果、版本、任务     | 对照、更新、重试     |
| 切片工作台 | 原文与切片双栏、参数、警告     | 试切、编辑、合并、发布  |
| 任务中心  | 阶段、进度、错误、重试次数     | 查看、取消、重试     |
| 检索调试台 | 查询、候选、排序、证据、Trace | 测试、比较、加入测试集  |
| 搜索页   | 筛选、结果、来源          | 搜索、打开原文      |
| 问答页   | 会话、状态、答案、引用       | 提问、停止、反馈     |
| 应用中心  | 应用、绑定知识、配置版本、密钥   | 创建、绑定、发布     |
| 评估中心  | 测试集、运行记录、指标对比     | 执行、评审、比较     |
| 企业设置  | 成员、角色、权限          | 邀请、禁用、授权     |
| 模型设置  | 模型配置、能力、连接状态      | 配置、测试、启停     |
| 套餐与用量 | 额度、消耗、明细、预警       | 查询、导出        |
| 审计日志  | 操作者、对象、动作、结果      | 查询、追踪        |

统一交互要求：

* 所有列表具备加载、空数据、失败、无权限状态。
* 高风险变更显示具体影响范围。
* 长任务允许离开页面后继续执行。
* 保存失败不丢失尚未提交的编辑。
* 切片列表和文档列表支持分页或虚拟滚动。
* 普通使用者不看到 Collection、索引参数等运维细节。
* 首期使用清晰的中文管理后台，避免只完成静态展示。

**16．核心数据对象**

以下是领域对象要求，不要求 Codex 一开始机械地为每项创建独立数据库表。

| 对象                 | 关键内容                 |
| ------------------ | -------------------- |
| Tenant             | 企业身份、状态、套餐           |
| Membership         | 用户、企业、角色、状态          |
| KnowledgeBase      | 元数据、责任人、默认配置         |
| ResourcePermission | 资源、主体、动作、授权版本        |
| Document           | 逻辑文档、当前发布版本、删除状态     |
| DocumentVersion    | 原文件、解析产物、有效期、来源摘要    |
| ChunkVersion       | 来源文本、检索文本、父片段、定位、修订  |
| ProcessingJob      | 阶段、幂等键、尝试、心跳、错误、用量   |
| IndexGeneration    | 模型身份、物理索引引用、构建状态     |
| Publication        | 有效内容清单、发布人、时间、回滚关系   |
| RetrievalProfile   | 召回、融合、重排、上下文和降级参数    |
| Application        | 应用身份、资源绑定、发布配置       |
| ModelConfiguration | 供应商、模型能力、版本、密钥引用     |
| ApiCredential      | 密钥摘要、应用、权限、状态、到期时间   |
| Conversation       | 用户、应用、消息、证据依赖        |
| Trace              | 请求、版本、阶段、耗时、结果、错误    |
| EvaluationCase     | 问题、证据、参考答案、身份、标签     |
| EvaluationRun      | 配置快照、指标、逐题结果         |
| UsageEvent         | 请求、资源类型、数量、计费规则、去重标识 |
| Entitlement        | 套餐权益、周期额度、调整记录       |
| AuditEvent         | 操作者、动作、对象、时间、结果      |

所有企业业务对象均包含租户归属；关键变更具备版本检查。已发布内容不可变，修改生成新版本。

**17．API 契约要求**

API 统一使用 `/api/v1` 前缀，提供 OpenAPI 文档。以下为资源语义，详细字段在开发设计阶段补全。

| 能力    | 接口示例                              | 关键约束           |
| ----- | --------------------------------- | -------------- |
| 知识库管理 | `/knowledge-bases`                | 授权、分页、版本冲突     |
| 文档上传  | `/knowledge-bases/{id}/documents` | 返回任务 ID，支持幂等   |
| 文档版本  | `/documents/{id}/versions`        | 草稿与发布版本分离      |
| 切片管理  | `/document-versions/{id}/chunks`  | 修改时带版本条件       |
| 发布    | `/documents/{id}/publications`    | 未就绪不能发布        |
| 任务查询  | `/jobs/{id}`                      | 当前阶段、错误、可重试性   |
| 检索    | `/retrieval/search`               | 返回证据，不强制调用生成模型 |
| 问答    | `/answers`                        | 支持流式输出         |
| 应用    | `/applications`                   | 资源绑定和配置版本      |
| 评估    | `/evaluation-runs`                | 异步运行、结果可追溯     |
| 用量    | `/usage`                          | 按应用、时间、资源类型查询  |

检索请求应能表达：

* 应用 ID。
* 查询文本。
* 在授权范围内选择的知识库。
* 类型化元数据过滤。
* 检索配置版本或应用默认配置。
* 结果数量，但不能超过服务器上限。

检索响应应包含：

* Request ID、Trace ID。
* 生效配置和知识发布版本。
* 证据 ID、文档及切片版本。
* 内容、标题、来源定位。
* 是否降级、警告和耗时。
* 分数与调试详情仅向具有权限的调用者开放。

问答流应具有明确的开始、状态、答案增量、引用、完成和错误事件。断开连接或重试不能静默产生重复业务扣费。

统一错误类型至少包含：

* 参数不合法。
* 未认证。
* 对象不可访问。
* 版本冲突。
* 文件超限。
* 配额不足。
* 请求限流。
* 处理未就绪。
* 模型或检索服务不可用。
* 请求超时。

无权访问的资源不返回其标题、数量或存在细节。

**18．套餐、用量与商业运营**

| 编号      | 需求                                            |
| ------- | --------------------------------------------- |
| BILL-01 | 套餐支持成员数、知识库数、文件空间、处理额度、查询额度、并发限制              |
| BILL-02 | 首期由管理员开通套餐，支持开始时间、到期时间、停用                     |
| BILL-03 | 分别记录 OCR 页数、Embedding Token、Rerank 用量、生成用量和存储 |
| BILL-04 | 用户可查看企业与应用的用量明细                               |
| BILL-05 | 达到额度阈值时预警，超限行为按套餐明确执行                         |
| BILL-06 | 手工调整额度必须记录操作者、原因和调整前后值                        |
| BILL-07 | 用量事件具有唯一标识，重复消息不重复扣费                          |
| BILL-08 | 导入前估算成本，执行后按实际用量结算                            |
| BILL-09 | 内部重试产生的供应商成本与客户账单分开记录                         |
| BILL-10 | 计费规则有版本，历史账单不随新价格变化                           |

并发请求应采用原子额度预占与结算，避免每个请求都认为余额充足而造成无限超额。

用户主动取消时，已实际消耗的资源按公开规则结算。系统内部重试不得默认为客户重复收费。

首期可以人工收款、后台开通，但不能把这一阶段描述为已完成在线支付和自动续费。

**19．安全、运维与非功能要求**

| 编号     | 要求   | 验收方式                         |
| ------ | ---- | ---------------------------- |
| NFR-01 | 租户隔离 | 跨租户对象、搜索、引用、任务、会话、密钥访问测试全部通过 |
| NFR-02 | 数据流向 | 企业可知哪些内容发送到外部模型；不允许外发时禁止对应配置 |
| NFR-03 | 密钥保护 | 不进入前端、普通日志和版本库               |
| NFR-04 | 上传安全 | 类型、大小、页数和解析资源限制；解析器不执行文件中的脚本 |
| NFR-05 | 可观测  | 请求 ID 串联上传、任务、检索、重排、问答和用量    |
| NFR-06 | 审计   | 权限、发布、删除、密钥、模型、额度变更可追溯       |
| NFR-07 | 故障恢复 | Worker 中断后恢复，不重复有效数据和扣费      |
| NFR-08 | 备份恢复 | 覆盖业务数据、文件、配置与索引重建资料，执行恢复演练   |
| NFR-09 | 数据删除 | 立即不可访问；活动存储异步清理；备份按保留期限淘汰    |
| NFR-10 | 资源隔离 | 企业并发、队列和文件限制，验证单租户负载不会无限挤占资源 |

默认数据保留策略可配置：

* 调试内容默认 30 天，企业可关闭正文采集。
* 审计元数据默认 180 天。
* 已删除资料在活动存储中默认 7 天内完成物理清理。
* 备份默认保留 30 天；恢复后必须重新应用删除和撤权记录。
* 法律或合同要求另行确定，不预先宣称合规认证。

首期不启用跨请求答案语义缓存。后续启用时，缓存必须包含租户、应用、权限版本、知识版本与模型配置，并保存证据依赖。

性能验收采用固定基准，建议起始目标：

| 项目           | 起始目标                                |
| ------------ | ----------------------------------- |
| 数据规模         | 10 万有效切片，至少 10 个测试租户                |
| 普通业务 API     | 不含模型及文件处理，P95 ≤ 500 ms              |
| 纯检索          | 不含外部查询 Embedding 和 Rerank，P95 ≤ 1 s |
| 完整检索         | 含查询 Embedding 与 Rerank，P95 ≤ 3 s    |
| 问答首个答案 Token | 含检索，P95 ≤ 5 s                       |
| 持续检索负载       | 从 5 QPS、10 并发请求开始测试                 |

这些是待验证目标。报告必须记录硬件、模型部署位置、网络、数据分布、冷热状态和失败率；模型供应商无法满足时，报告瓶颈并调整方案，不能修改统计口径掩盖问题。

正式生产可用性 SLA 在部署和恢复能力验证后确定。

**20．端到端验收用例**

| 编号    | 操作          | 通过标准                                   |
| ----- | ----------- | -------------------------------------- |
| AC-01 | 上传 PDF 并发布  | 完成真实解析、切片、向量化、索引，能够检索                  |
| AC-02 | 上传扫描 PDF    | OCR 结果可查看，错误可修订                        |
| AC-03 | 修改切片        | 草稿不影响线上，发布后新内容生效                       |
| AC-04 | 上传新版文档      | 新版发布前旧版可用，发布后不混用同文档版本                  |
| AC-05 | 回滚          | 内容恢复，撤销的权限不会恢复                         |
| AC-06 | 同义问题检索      | 能找到标注的正确证据                             |
| AC-07 | 型号与错误码检索    | 保留关键标识，支持必要的精确过滤                       |
| AC-08 | 查看混合检索过程    | 能分别看到两路召回、融合和模型重排结果                    |
| AC-09 | 问不存在的内容     | 明确无依据，不捏造资料引用                          |
| AC-10 | 点击引用        | 定位实际支持内容，版本与本次证据一致                     |
| AC-11 | 跨租户访问       | 文档、切片、任务、会话和接口均不能泄露内容                  |
| AC-12 | 撤销文档权限      | 新请求被限制，历史相关答案被隐藏                       |
| AC-13 | 删除后重放任务     | 文档不能重新出现，清理状态可追踪                       |
| AC-14 | Worker 中断重启 | 任务可恢复，不重复数据或扣费                         |
| AC-15 | Rerank 超时   | 按配置明确降级或报错，不假装重排成功                     |
| AC-16 | 两个应用复用知识库   | 应用配置独立，知识内容共享，权限不扩大                    |
| AC-17 | 额度边界并发请求    | 不出现无限超额和重复扣费                           |
| AC-18 | 评估配置变化      | 同一测试集可比较结果和失败案例                        |
| AC-19 | 从备份恢复       | 恢复后可用，并维持最新删除和撤权约束                     |
| AC-20 | 真正运行依赖      | 提供真实 Milvus、Embedding、Rerank 和生成模型链路证据 |

建议试点质量目标：

* 已标注可回答问题的 Hit@10 ≥ 90%。
* 无答案问题正确拒答比例 ≥ 90%。
* 人工评审的关键结论证据支持比例 ≥ 90%。
* 权限越界验收用例全部通过。

质量目标绑定测试集和判定标准，不可外推为所有客户数据的准确率。

**21．开发里程碑**

| 阶段         | 交付内容                            | 完成条件            |
| ---------- | ------------------------------- | --------------- |
| M0：设计基线    | 仓库检查、架构决策、版本选择、对象模型、API 契约、任务拆分 | 能解释边界、依赖和关键风险   |
| M1：基础与导入   | 企业身份、权限、知识库、上传、任务、真实解析          | 文件可追踪地处理并显示结果   |
| M2：切片与索引   | 工作台、版本、真实模型、Milvus、发布与回滚        | 文档可从草稿进入可检索状态   |
| M3：检索与问答   | Hybrid Search、Rerank、调试、搜索、引用问答 | 完成可核验的端到端链路     |
| M4：应用与商用管理 | 应用、密钥、配额、计量、审计                  | 不同应用可独立调用且用量可追踪 |
| M5：质量与交付   | 测试集、隔离验证、故障恢复、性能与备份报告           | P0 验收完成，限制明确记录  |

每个阶段都应交付可运行增量。不能先完成全部页面，再把真实集成留到最后。

最终工程交付必须包括：

* 可运行源码。
* 数据库迁移。
* 环境配置示例，不含真实密钥。
* 本地启动与部署说明。
* OpenAPI 文档。
* 核心边界的架构决策记录。
* 脱敏或合成的演示资料及测试集。
* 验收报告、性能报告和已知限制。
* 备份恢复步骤与验证记录。
* P0/P1/P2 需求完成状态表。

**22．交给 Codex 的执行指令**

将本 PRD 保存为 `PRD.md`，可附上下面这段指令：

> 请依据 PRD.md 开发可扩展企业知识库平台。
>
> 先检查现有仓库及其开发规范，核对已有能力与 PRD 的差异。若是空仓库，采用 PRD 中的默认技术方案；若已有成熟实现，优先评估复用。
>
> 先完成 M0：记录架构决策、兼容版本、核心数据对象、API 契约及开发任务，并将需求编号关联到任务。随后按 M1—M5 顺序推进，持续交付可运行增量，不在输出计划后停止。
>
> 本次只实现 P0。不要提前开发业务意图识别、业务工具执行、知识图谱、插件市场或在线支付。意图识别只预留文档化接口；知识库不得依赖具体业务。
>
> 必须真实接通文档处理、Embedding、Milvus、BM25 与向量混合检索、模型 Rerank 和引用问答。模拟适配器只能用于明确标识的测试环境，不能作为集成完成证据。
>
> 对权限隔离、撤权、发布版本、任务幂等、删除一致性和计量准确性进行针对性验证。缺少模型密钥等外部条件时，完成其余可执行工作，明确记录阻塞项，不编造成功结果。
>
> 常规实现选择按照 PRD 默认值推进并记录理由；涉及不可逆数据迁移、对外发布或新增付费资源时，完成可审阅方案后按环境授权要求处理。
>
> 每完成一个里程碑，报告已完成的需求编号、验证结果、未完成项和真实限制。最终提交启动说明、API 文档、验收报告和需求完成状态表。
