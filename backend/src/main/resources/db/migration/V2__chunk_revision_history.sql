CREATE TABLE chunk_revisions (id VARCHAR(36) PRIMARY KEY, tenant_id VARCHAR(36) NOT NULL, chunk_id VARCHAR(36) NOT NULL, revision BIGINT NOT NULL, previous_content TEXT NOT NULL, actor_id VARCHAR(36) NOT NULL, reason TEXT NOT NULL, created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP);
