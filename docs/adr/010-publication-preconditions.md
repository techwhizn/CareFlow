# ADR 010：发布校验文档与内容版本两种修订

状态：采纳。

仅检查文档revision可以阻止两个发布互相覆盖，却不能发现“预览之后编辑内容并重新索引”的变化。发布因此要求额外提供version_revision，在租户事务锁内比较权威内容修订与READY状态。旧预览拒绝发布，用户重新核对。

这是仓库预览API/SDK的必填参数变更；不自动读取最新修订填充，否则失去预览冲突保护。新发布保存双修订与上次版本作为审计证据，历史缺失值保持未知。原文件处理、内容发布和应用发布仍各自独立。
