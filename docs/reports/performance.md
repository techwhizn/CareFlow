# 性能报告状态

状态：**未执行 PRD 性能基准，尚无达标结论**。

已经验证本机开发依赖能够启动及小型合成资料可真实导入。没有将单个健康检查或小文件处理时间解释为生产性能。

待测条件：10万有效切片、至少10个测试租户、5 QPS起步、10并发、明确硬件与供应商部署位置、冷热缓存、网络情况和失败率。分别记录普通API、纯检索、Embedding+Rerank全检索、问答首Token的P50/P95。

当前阻塞：真实模型服务未配置；尚无达到基准规模并经标注的数据。`worker/careflow/evaluation.py` 可对已标注资料请求真实API并输出延迟和失败率，不能替代正式压测。

PRD 起始目标保持不变：普通API P95≤500ms、纯检索≤1s、完整检索≤3s、首Token≤5s。没有改变统计口径以宣称达标。
