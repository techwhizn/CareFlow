# V1-01 模型配置与安全管理

2026-09-10：已完成本工作包的连接注册范围。新增Java控制器/服务/Repository/Vault、V4迁移和模型配置页面。

验证：Java28项测试通过（新增5项）；前端TypeScript/Vite构建通过；Python初始化脚本Ruff检查通过。新测试覆盖密钥随机加密、AAD绑定、缺主密钥失败、响应不回显、审计、更新保留密钥、revision冲突、跨租户与普通用户拒绝、地址绕过及Embedding元数据约束。

真实MySQL8.4执行V4迁移成功；浏览器登录、创建“验收示例（未绑定）”、列表及编辑回显通过；示例未填写密钥且未调用模型。运行OpenAPI已重新导出。

边界：注册不负责真实模型连接或知识库配置绑定（V1-02/03）；现有环境模型继续工作。主密钥轮换/备份恢复、完整网络出口验证及生产安全审计仍由后续任务验收。Flyway对MySQL8.4的兼容提示仍存在。
