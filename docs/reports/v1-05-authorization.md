# V1-05 多主体授权与权限界面

2026-09-10：新增AuthorizationService/Controller，移出综合管理控制器的权限业务逻辑；前端独立AclDialog加载当前多主体授权、逐项编辑、明确全量替换并处理版本冲突。新增文档权限读取与授权编辑快照。

验证：Java37项测试通过（新增3项）；前端严格检查与构建通过。新增覆盖多主体快照、旧revision冲突、文档不能扩权、非法/已移除主体导致事务完整回滚。真实MySQL合成企业中创建两测试主体，保存三条权限，快照revision=1；重复旧revision请求返回409。OpenAPI已同步。

保留约束：有效访问仍取身份、知识库、文档及应用绑定交集。UI保存可能使当前操作者失去文档管理权，需显式核对。企业/应用引用概览另由V1-08完成；完整跨入口权限矩阵仍由V1-07验收。本次未修改真实业务企业的授权。
