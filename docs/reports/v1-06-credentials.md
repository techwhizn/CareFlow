# V1-06 应用凭证范围、有效期与轮换

2026-09-10：新增应用凭证服务/控制器与V7 scopes迁移。可选择READ/SEARCH/ANSWER及1～365天期限；元数据列表不返回密钥或摘要。应用内可签发与撤销，轮换采用先签发、迁移验证、再撤销旧密钥。scope与当前认证身份绑定，入口在业务处理前检查。

验证：Java39项测试通过（新增2项）；前端严格检查与构建通过。覆盖SEARCH-only不能问答或GET、撤销立即失效、非法scope/期限、过期、跨应用撤销拒绝以及无请求体的90天兼容默认。

真实MySQL执行V7迁移成功。在合成测试企业创建并发布应用，签发SEARCH-only测试密钥：搜索200、读取/问答404；撤销后搜索401。测试密钥未打印且已撤销；既有用户密钥未修改。OpenAPI已同步。

READ表示通过原有资源授权校验的GET能力，不取代资源ACL；应用不能通过SDK或用户ID字段扩大身份。个人凭证仍由成员角色控制。完整跨入口授权、外部身份委托限制及生产安全验证继续由V1-07覆盖。
