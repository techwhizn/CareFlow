# V1-08 知识库属性与概览验证

日期：2026-09-10。范围：知识库名称/描述/语言/标签/责任人、授权统计、绑定应用关系；不代表整个产品验收。

## 实现与验证

- 属性更新在服务层校验启用责任人、标签与修订；移交改变隐式权限，显式权限保留。列表/创建/读取迁至独立控制器、服务和Repository。
- 统计逐文档/版本授权，当前发布启用切片、可管理文档失败任务、原文件对象去重；应用关系遵守应用管理角色。未知历史文件大小单独展示。
- Java Spotless与Maven verify通过，42项测试，0失败/错误/跳过。新增3项覆盖移交后的权限、旧revision不覆盖、非法责任人/空标签回滚、受限文档不计数、草稿共享对象不重复计量、普通读者不可见应用和失败任务、撤权后概览404。H2组件测试不代替MySQL并发验证。
- 前端TypeScript严格检查及Vite构建通过。
- 本机MySQL 8.4执行V8迁移成功；使用此前合成企业A/B新建一个标记测试库，验证属性/标签保存、重复revision409、空概览和企业B访问404。无外部模型调用。
- 本地浏览器实际登录合成企业A，打开概览、加载设置回显（en-US和标签）、保存描述，返回列表出现新描述与revision2。
- 从本次运行的Java服务刷新OpenAPI；API说明和ADR同步。未推送Git、未创建Release或对外部署。

## 限制

原文件空间不是索引/备份/账务总量；V8以前的文件大小保持未知，不能冒充零。概览逐项校验适用于当前增量，规模压测和性能优化在V1-45执行。属性编辑不是模型配置版本发布。远端CI未在此项执行。
