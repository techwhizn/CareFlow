# V1-10 批量上传、类型校验与重复提示

2026-09-10：上传逻辑移入DocumentUploadService，新增UploadValidation早期签名/UTF-8检查与V5请求指纹。批量上传UI独立为DocumentUpload组件，每批最多20个，每个最大50MiB，逐文件展示结果并在失败后继续。

验证：Java31项测试通过（新增3项，已有同请求重放测试继续通过）；前端严格检查和生产构建通过。覆盖扩展名伪装、无效UTF-8/空文件、摘要、同key不同内容409、同内容新key重复提示。

真实MySQL执行V5成功。浏览器在合成资料知识库上传first.txt、invalid.pdf、last.txt：两个文本成功保存MySQL/S3，中间伪装PDF被拒绝，后续文件继续上传；页面三条独立结果正确。该次测试只验证上传接收，不宣称解析/索引/发布完成。Worker已有损坏/加密输入的解析拒绝用例沿用。

服务端保留逐文件API，批次是UI最多20次顺序请求；不是跨文件原子事务。V5之前的历史任务没有请求指纹，重复请求安全返回冲突，需核对原任务。深入解析、OCR效果和解析资源隔离仍由V1-12/13/44验收；上传外部存储与长事务的补偿调整仍属于V1-14。
