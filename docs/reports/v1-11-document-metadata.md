# V1-11 文档元数据与有效期验证

日期：2026-09-10。只覆盖本工作包，不代表真实检索问答全链路完成。

## 完成内容

独立Java控制器/服务/Repository实现标题、来源、语言、标签、型号、带时区有效期与修订历史。Web支持查看、编辑、冲突后重新加载和分页查看前后快照。来源只保存文本。V9新增文档字段及历史表；旧记录不伪造历史。

有效期在默认召回范围与每次证据复核中执行，影响外发、流式继续交付和历史答案。与文档版本现有有效期取交集。UTC归一化DATETIME支持2038年以后；本地时区只用于显示。

## 实际验证

- Java Spotless/Maven verify通过，44项测试，0失败/错误/跳过。新增有效期变更使已保存答案隐藏、过期证据拒绝、默认召回排除、恢复有效期后重新可用；时区归一化、2999年日期、格式/无时区/反向日期拒绝、旧revision冲突且无额外历史。跨企业用例新增元数据及历史入口。
- TypeScript严格检查及Vite生产构建通过。
- 本机MySQL 8.4新增V9迁移成功。在合成企业A知识库上传一个明确测试文本并更新元数据；验证UTC回显、2999年有效期、409冲突、历史记录、企业B读取404。上传已进入解析队列，此次不宣称解析完成。
- 浏览器打开测试文档，验证标签/CF-100型号、UTC表单与本地时区显示，保存来源字段；API再次核对修订历史。
- OpenAPI从当前本地Java运行服务导出。API文档和ADR005同步。

没有调用真实Embedding/Rerank；没有执行Git推送、Release、公开包或镜像发布。类型化召回过滤属于V1-25。
