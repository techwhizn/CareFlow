# V1-12 结构解析与来源验证

日期：2026-09-10。范围：六种文本/Office/PDF格式的结构与来源，OCR单独验收。

## 完成内容

修复DOCX先输出全部段落再输出表格造成的顺序丢失；保留标题路径、列表标识和段落/表格/行列位置。补合并表格警告、CSV非齐整列警告、XLSX完整单元格范围和未计算公式标识。Markdown正确跳过fenced代码中的标题，修复跳级标题路径。PDF使用layout文本提取并提示复杂布局核对；重复页边文字清理保留源文本和忽略区间。

格式适配与清洗拆入独立Python模块；未引入依赖。原文/清洗内容/位置/警告继续使用现有Java切片协议与工作台展示。完整规则见docs/parsing.md。

## 验证证据

- Ruff静态检查与格式检查通过。
- Python测试25通过，1跳过（需显式启用的真实Milvus测试，与本次解析修改无关）。新增7项真实库生成的DOCX/XLSX/PDF以及Markdown/CSV/清洗样本，验证结构、顺序、列坐标、公式不执行、警告和原文范围。现有Unicode/Token预算/解析拒绝回归通过。保留AnyIO上游弃用警告。
- 本地真实上传合成DOCX，经Java、MinIO、RabbitMQ、Python进程解析并回写Java，任务DONE；4个切片按标题、列表、表格、后续段落顺序返回，表头单位与列坐标正确。
- 没有生成索引或调用模型；没有宣称复杂表格精确还原。未推送或发布制品。
