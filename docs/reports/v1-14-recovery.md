# V1-14 上传一致性与任务恢复验证

日期：2026-09-10。范围：暂存补偿、幂等、租约/检查点、取消；不代表所有存储灾难与生产恢复场景完成。

## 实现

上传短事务登记、事务外S3、短事务重新认证/授权并挂接版本、任务与Outbox。失败/竞争产生的未挂接对象有到期回收、租约重试和清理审计；已挂接版本受保护。对象存储调用有超时。新增V10迁移，不改动历史迁移。

阶段检查点和最近心跳在任务中心显示；任务恢复统一企业/任务锁顺序，总计最多3次，不重试已知永久解析/配置错误。取消清空租约、封锁回调，Worker解析子进程检测租约丢失后终止。

## 实际验证

- Java Spotless/Maven verify：49项通过，0错误/失败/跳过。新增5项覆盖事务外写存储、写入期间撤凭证不能挂接、失败存储不产生有效文档、补偿清理失败后重试、精确重放不重复写S3、已挂接对象不误删、检查点单向/心跳/取消/迟到回调、租约最多3次和永久错误不重试。存储故障使用测试替身，未称为真实S3故障演练。
- Python Ruff通过，32项通过、1项显式Milvus跳过；包含真实独立子进程解析、超时与取消回收测试。AnyIO上游弃用警告仍存在。
- TypeScript严格检查和生产构建通过。
- 本机MySQL V10迁移成功；真实MinIO/RabbitMQ/Worker场景中两个并发相同上传返回同一任务；任务有heartbeat_at，最后checkpoint=DONE；公开响应不含租约/请求键/上传指纹。
- 真实长文本解析进入RUNNING/SOURCE_READY后取消，状态CANCELLED；Worker下次心跳404，迟到写回由Java租约校验拒绝。没有调用Embedding/Rerank模型。
- 运行时OpenAPI、API说明、运维说明与ADR006同步；仅本地提交。

## 边界

暂存清理只覆盖V10以后的登记对象，历史孤立对象与正式删除保留策略仍属V1-23。检查点不恢复解析进程内存，重试从任务安全起点开始；已发出的模型成本不能回滚。Docker Hub问题使容器OCR验收仍待V1-13，不能用这里的主机测试替代Linux内存验证。完整恢复/规模/远端CI仍待对应任务。
