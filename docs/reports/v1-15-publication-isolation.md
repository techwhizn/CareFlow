# V1-15 文档发布隔离验收

日期：2026-09-10。新增双修订发布前置条件、发布历史快照与界面，以及源版本未解析完成时禁止复制草稿。新版文件仍独立创建版本，旧发布内容保持不可变。

已执行：Java 71项测试通过；新增覆盖预览后修改并重新就绪仍拒绝旧内容修订、发布历史与回滚、缺少内容修订400、处理中复制409、两请求并发发布只有一个成功、新版上传不改旧发布。测试中模拟READY状态明确仅用于组件边界，不冒充真实向量索引。

Java SDK 5项、Python SDK 17项通过，同步/异步支持读取版本修订，发布必填内容修订；前端构建、Ruff通过。预览SDK签名变更见 [SDK说明](../../sdk/README.md)。

本机MySQL成功升级V14；真实PythonSDK读取合成文档修订，Java拒绝缺失修订与未索引发布，历史查询返回空列表。浏览器确认未索引版本发布按钮禁用、原文和修订内容可见、新发布历史空态正常。公共OpenAPI从真实服务刷新。

后续真实联调已通过：从实际发布版本复制草稿、编辑后旧版仍可检索；使用真实Embedding与Milvus重建索引，旧内容修订发布返回409；正确修订发布后证据切换新版本；回滚后恢复旧版本且旧切片保持不变。见 [真实多阶段报告](v1-15-24-25-real.json)。V1-15验收完成。历史列迁移与客户端兼容变化见 [发布说明](../document-publications.md)。未推送或发布。
