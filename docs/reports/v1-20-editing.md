# V1-20 人工编辑与解析冲突验收

2026-09-10，本机合成数据。操作和限制见[编辑契约](../chunk-editing.md)，架构与数据库时间策略见[ADR 013](../adr/013-content-review-and-database-clock.md)。

[真实链路记录](v1-20-mutations-real.json)通过真实Java/MySQL及BGE Tokenizer验证拆分完整文本、相邻合并、正文修订、两片段标签和禁用，5次操作均有操作者、原因及修改前快照。重新解析冻结3项旧人工修改，索引和发布均409 UNRESOLVED_CONTENT_CONFLICTS。

浏览器对照旧原文、人工正文和原文相同的新片段，采用核对过的修订后，新片段正文与标签正确，显示处理记录。随后完成其余冲突，保留禁用状态和明确无当前文件定位的人工补充；真实索引、发布及混合检索成功，已发布版本的批量修改409 IMMUTABLE_PUBLICATION。另在复制草稿中通过浏览器同时修改两个片段标签，并从Java API验证结果。

时间验收发现JVM Asia/Shanghai与MySQL UTC不一致造成TIMESTAMP偏移。修复连接设置后，原审计记录在浏览器显示20:49:19，与实际操作时刻一致。[时钟记录](v1-20-clock-real.json)比较新建临时凭证的请求到期时刻、API读回与MySQL UNIX_TIMESTAMP，误差小于1秒，临时凭证已撤销。历史Java写入的到期值不作猜测性批量迁移，升级核对要求已文档化。

Java后端108项通过，含原子回滚、陈旧修订、代理对边界、模型预算、凭证中途撤销、已发布保护、冻结冲突和FAQ标签/启停保留。Java SDK10项、Python SDK24项通过；Web7项原生测试和TypeScript/Vite构建通过；Spotless、Ruff和差异检查通过。本项无Worker处理协议改动，未重复宣称新增OCR或完整性能验收。

真实MySQL已从V18升级到V20。工作包完成不等于全部1.0完成；增量向量复用、索引重建/清理与检索上下文扩展按后续任务推进。切片标签是管理标签，现有公共检索元数据过滤仍按文档标签契约，不冒充已支持独立切片标签检索。
