# V1-21 增量处理验收

日期：2026-09-10。合成数据，真实MySQL 8.4.6、Milvus 2.6.1、BGE small zh v1.5（512维、固定修订），本地Java与Worker consumer。

[实际记录](v1-21-incremental-real.json)：首次3条输入96 Token；新草稿只修订一条，新算1、复用2、48 Token；仅改标签，新算0、复用3、零Embedding调用。未发布的草稿索引不改变原发布检索结果。真实dense和原生BM25命中同一条新切片，Milvus全部草稿记录与Java启用切片文本和维度一致。其他租户读取任务返回404。

浏览器任务中心核对三次处理计数、实际Token和零调用，历史无台账任务显示“—”。没有通过页面存在推断接口通过。

自动化验证：Java Maven verify 109测试、0失败；Worker 89通过、1个默认跳过的独立Milvus集成测试；Python SDK 24通过；Web 7测试及生产构建通过；Ruff和Spotless通过。此次真实Milvus证据来自上面的实际链路，不将跳过的测试记作通过。

行为回归覆盖跨租户/模型修订不复用、相同文本复用、成功批次在后续失败后保留、缓存内容损坏拒绝、未知/非法供应商用量不伪造为0、错误向量前先保存用量、重复回报不重复计量、任务取消后回报既有调用但不能开启新调用或恢复租约、完成计数/用量不匹配拒绝。

限制：旧索引第一次增量处理会重新计算并填充缓存。并发缓存未命中可能分别产生调用，均按实际记录。V1-22的完整代际核对、V1-23的物理清理和V1-38的全资源结算仍单独待做；本报告不是1.0整体验收。
