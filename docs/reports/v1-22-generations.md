# V1-22 索引核对与代际重建

日期：2026-09-10。真实MySQL 8.4.6、Milvus 2.6.1、固定BGE 512维模型、本地Java及Worker；仅使用合成资料。

[实际记录](v1-22-generations-real.json)包括：

- 旧索引全部3条内容核对，重建为新代际后继续检索；相同内容全部复用向量。
- 真实新代际写入Milvus后，故意回报错误清单摘要，Java返回409且旧代际仍可检索。任务回报可重试失败后自动领取第二次尝试，使用另一代际恢复，失败副本未被激活。
- 对合成代际分别注入删除一条、额外插入一条、修改一条文本、修改一条向量。完整核对准确报告缺失1、多余1、内容/向量异常2。
- 浏览器显示上述差异，从页面提交重建原因、建立新代际，再点击逐项核对；当前代际业务/索引均3条，差异归零。失败和旧代际记录保留。
- 新草稿首次索引走新代际流程并核对通过；原发布搜索不包含草稿。另一租户查看核对历史或发起核对均404。

组件检查：Java 112测试、0失败/错误；Python Worker 92通过、1个默认跳过的独立Milvus测试；Python SDK 25通过；Java SDK 11通过；Web 7测试及构建通过，Ruff/Spotless通过。Worker和Java的行为替身测试覆盖完整分页核对、不同内容/向量/物理ID、过期租约、重试代际、摘要校验、撤销凭证及归档边界。真实证据使用上述实际链路，不把测试替身或跳过测试当作真实Milvus验收。

文档发布及回滚继续由`DocumentPublicationService`按当前授权和修订校验，代际重建不写入权限表；相关发布隔离证据见V1-15。新代际激活是索引指针切换，不代表文档或软件对外发布。

V22迁移已经实际应用于本地MySQL。历史集合继续可读，首次成功重建后转入新代际；发布配置、租户授权和正文不被自动改变。失败/退休代际的物理回收属于下一项V1-23，此报告不宣称该清理已经完成。
