# V1-23 物理清理验收

日期：2026-09-10。本机MySQL V23、Milvus 2.6.1、MinIO及真实BGE模型；全部资料为合成测试数据。结构化结果见 [真实报告](v1-23-cleanup-real.json)。

创建两份具有共享切片的文档并建立真实索引。删除第一份立即返回不可见，后台等待120秒后执行持久化清理；最终版本与文档请求均DONE，切片和缓存归属已清除，真实Milvus没有该版本索引，独占缓存消失、另一份文档的共享缓存和原文件保留。已删除文档原文件全部S3版本核对为空；另一个企业看不到清理记录。重放已删除任务领取返回409。

验收中发现Milvus无可压实计划返回-1，修复后原请求自动从VECTOR_DELETE恢复；历史2次失败保留，未改写成功或创建替代任务。对实际返回的正数计划等待Completed，-1表示服务端没有生成计划，不伪造可轮询任务ID。该行为依据 [Milvus 2.6.1 ManualCompaction](https://github.com/milvus-io/milvus/blob/v2.6.1/internal/datacoord/services.go)，已有回归测试。

在独立临时版本化S3桶调用正式BlobStore.purge：两个历史版本和一个删除标记全部删除，重复清理幂等，同名前缀的另一对象保留；测试桶完成后移除。正式MySQL备份脚本成功生成受管快照；保留期CLI删除31天前的合成备份并保留近期/未知目录。浏览器任务中心实际展示清理期限、阶段和保留的失败次数。

检查：后端119项通过（新增7项包含存储故障重试、失效租约、KB级联、当前代际/共享缓存保护、管理API隔离），Worker100项通过、真实Milvus单元套件中的独立集成入口1项按环境跳过，Python SDK26项通过，Java SDK12项通过，Web7项测试及生产构建通过，Ruff/Spotless/diff检查通过。Worker回归套件合计与Python SDK合跑126通过、1跳过。

边界：DONE是应用数据删除核对及可用压实计划完成，不等于磁盘安全擦除。Milvus旧段按GC窗口回收；外部卷/S3备份需部署运维设置30天保留，受管MySQL快照由脚本淘汰。未将等待实际30天或7天墙钟时间作为测试；边界由合成时间测试和实际命令验证。恢复后的删除撤权重放、RPO/RTO仍属于V1-46，未宣称完整恢复验收。
