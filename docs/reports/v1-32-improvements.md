# V1-32 反馈与知识改进专项验收

日期：2026-09-10。仅本地专项验收。

## 实现

V26 新增反馈原因/说明/修订、查询记录和改进任务。检索保存可追溯配置、范围与证据身份，差评和无结果可提交任务，重复提交复用，支持负责人、状态与处理说明。搜索业务、反馈和任务规则由各自服务负责，AnswersController 不再保存 SQL。

页面提供反馈表单、无结果转任务和知识改进工作台。Java 与 Python 同步/异步 SDK 提供对应方法。角色和资料授权在每次任务打开/处理时重新校验；物理清理级联清理派生记录。

## 已执行检查

后端 149 项通过，新增 3 项覆盖任务去重、反馈/处理修订冲突、读者无权关闭、跨租户、撤权隐藏、负责人完整资料权限和物理清理级联；可用/降级/服务不可用查询不能冒充无结果。Web 13 项通过并完成生产构建。Worker 未改变，本项沿用 V1-31 的 121 项通过、1 项可选测试跳过结果。

Python SDK 30 项、Java SDK 16 项通过。真实 DeepSeek 回答记录反馈后可转任务，查询/配置/证据身份可回溯；重复建任务返回同一 ID，跨租户和读者越权处理返回 404，过期修订返回 409。管理员分配、处理中、关闭成功。实际 keyword 空召回可单独建任务。MySQL 事务内验证删除查询记录会级联删除任务，随后回滚确认任务恢复；没有为此删除验收源文件。

浏览器打开“知识改进”，查看无结果原始问题与配置、修改为处理中、保存说明，列表立即反映新状态。真实记录见 [JSON](v1-32-improvements-real.json)。

限制：关闭任务只记录管理员决定，不自动验证知识修订质量或发布状态。跨浏览器和完整发布验收仍由后续工作包负责。
