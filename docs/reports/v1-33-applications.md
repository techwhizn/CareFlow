# V1-33 应用配置专项验收

日期：2026-09-10。仅本地专项验收。

实现应用负责人、独立查询模型快照、检索策略与回答约束，新增 V27 发布指针/历史。抽取共享 ModelSnapshotService，应用与知识库不重复实现凭证快照。页面提供配置表单和应用选择；Java/Python SDK 提供版本接口。

后端 151 项通过，含两个应用共享知识但模型和约束独立、旧模型快照不随档案修改、策略模式约束、回滚和撤权失败测试。Worker 122 项通过、1 项可选测试跳过，新增历史上限和受控输出策略测试。Python SDK 31 项、Java SDK 17 项通过；Web 13 项通过且构建成功。

真实 MySQL 已应用 V27；两个应用共享合成 CF-900 知识，实际 DeepSeek 分别输出英文简洁和中文详细答案。英文应用同时绑定另一个查询配置不同的知识库，使用独立模型策略正常检索，原索引身份保持不变。APP 调试内部轨迹未暴露；实际 start 返回各自 0/600 历史 Token 上限。

保存新草稿未改变当前发布指针；发布及回滚形成修订 1、2、3；撤销其中一个知识库授权后回滚返回 409。恢复合成授权后保留测试环境。结构化证据见 [JSON](v1-33-applications-real.json)。

浏览器打开英文应用，载入已发布的两个知识库、BGE Rerank、DeepSeek Flash、英文/简洁和零历史配置；修改历史限制为 1 轮/300 Token 并保存草稿，页面仍显示原配置为当前发布、应用修订仍为 3。

限制：语言/风格是模型偏好，不能替代内容质量评估；低输出预算可能产生明确的未完成回答。跨浏览器验收仍归 V1-49。
