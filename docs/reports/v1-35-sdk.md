# V1-35 Java/Python SDK契约验收

2026-09-11：完成本工作包。SDK保持独立HTTP客户端，管理、文档、版本、来源下载、配置、查询、任务及应用能力由公共Java API授权。核心响应具有类型，扩展管理响应保留动态字段；这不是所有OpenAPI响应均已强类型化的声明。

两种语言在同一独立合成知识库完成上传、解析、真实索引、发布、任务取消、混合搜索、完整DeepSeek SSE及首段后取消。取消后服务端日志均为CANCELLED。[生命周期记录](v1-35-sdk-lifecycle-real.json)与[同原文SHA256、跨租户404记录](v1-35-sdk-read-real.json)分别保存证据。Java首次执行的包装器未处理JVM警告前缀，SDK进程已成功退出；随后通过公共API核对任务和发布状态，没有重复上传。

| 验证 | 实际结果 |
| --- | --- |
| Python3.10 / 3.12 / 3.14 SDK行为测试 | 各37通过 |
| Java21隔离容器clean verify | 23通过 |
| Java25本机verify、格式检查 | 23通过 |
| 双语言实际网络协议 | Java21、Python3.12通过 |
| 后端OpenAPI快照 | 公共路径、参数、引用类型漂移检查通过 |

行为测试覆盖认证错误、404、409、429、结构异常、SSE错误与连接关闭；真实权限验证不使用模拟服务。Java SDK运行不依赖Spring。单次合成生命周期不代替压力、浏览器或升级恢复验收。尚未发布SDK包。
