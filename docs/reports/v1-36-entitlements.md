# V1-36 套餐资源限制验收

日期：2026-09-10。范围：BILL-01、BILL-02、BILL-05。仅本地验证与提交，不推送或发布。

## 已实现

Java 服务层统一管理成员、知识库、文件空间、累计处理/查询、并发、PDF 页数限制；管理员通过版本化接口和后台表单人工调整开始/到期、停用状态与阈值。调整有前后值、操作者和原因审计。资源新增与额度检查在租户锁内执行；上传采用空间预占和最终二次检查。

预警显示于后台与接口。超限新增请求拒绝，已有资源不删除。内部任务重试只占一次处理额度。单位、保留读取规则、累计不自动清零、暂存与旧文件空间口径见 [套餐说明](../entitlements.md)。

## 已执行

- Java `spotless:apply verify`：61 项通过；新增覆盖套餐停用/到期/恢复、日期与修订冲突、非管理员隐藏、旧文件空间预留、成员/知识库/上传超限先于 S3、任务并发与重试不重扣、并发查询失败释放。
- Python：38 项通过、1 项真实 Milvus opt-in 跳过；新增真实子进程 PDF 租户页数限制与父进程配置隔离测试。Ruff lint/format 通过。
- 前端 `npm run build --prefix web` 通过。
- 本机 MySQL 8.4.6 成功应用 V12；合成企业停用后新建知识库返回 ENTITLEMENT_INACTIVE，旧修订返回 409，再恢复初始配置成功。
- 真实浏览器读取资源额度、提交套餐表单；API 复核修订号推进与对应中文原因审计记录。未更改合成企业原有资源上限。
- 真实 MinIO/RabbitMQ/消费者：合成 2 页 PDF 在租户上限 1 页时返回 INVALID_FILE，处理计数增加 1；确认限制从 Java 领取契约传到隔离子进程，随后恢复原套餐配置。该测试不调用 OCR。
- 公共 Java OpenAPI 已从运行服务刷新；内部任务 v1 契约补 PDF 限制，Python 消费端透传到子进程。

## 限制

本次行为测试的 Java 部分使用 H2、S3/Worker mock，MySQL 仅补上述实际 API 场景，不宣称全资源真实并发压力验收。真实 OCR/Linux 容器限制、Embedding/Rerank、计量明细、物理删除与队列公平调度仍属于 V1-13/02/37/23/44。预警为页面与 API 状态，不包含外部通知。没有自动周期结算或真实模型账单验收。

默认迁移限制与回退说明见 [套餐说明](../entitlements.md#迁移与恢复)。
