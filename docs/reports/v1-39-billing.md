# V1-39 费用预估与核算验收

2026-09-11，本机真实 MySQL V33、MinIO、Worker、BGE 与现有知识库执行合成数据验收。价格是明确标记的测试配置，已恢复原先未配置状态。未发布文档、项目或软件包。

- 原生 Tokenizer 预估短文本，返回源文件字节、OCR/Embedding 估计及规则修订。
- 从规则 A 切至 B 后，旧预估上传返回 409；重新预估后上传解析成功。
- 旧查询客户费保持 0.1，新查询为 0.2；实际模型 Token 按相应版本核算。
- 解析和索引任务分别冻结规则，源文件写入仅计入新上传任务；索引使用实际回报 Token。
- 应用凭证费用接口隐藏内部资源成本；管理员可查看两类费用。
- 浏览器检查规则历史、每项单价和调用成本。实际请求显示客户费用 0.2、内部资源成本 0.000356，与 API 一致。未在浏览器执行文件选择器上传；真实上传使用公共 SDK。

结构化结果见 [真实报告](v1-39-billing-real.json)。后端 169 项测试、Python SDK 34 项、Java SDK 20 项、前端 13 项测试通过，前端构建通过。规则未知、用量未知、历史冻结、上传前拒绝写入、角色成本可见范围均有行为测试。未将局部验收表述为完整发布验收。
