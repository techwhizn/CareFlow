# V1-44 公平调度验证

日期：2026-09-10。本地开发与验证，不推送或发布。

实现持久化企业轮转、全局32个通知预留上限、每企业一个待领取通知、套餐与并发二次准入、120秒通知过期重试和迟到确认隔离。Worker 对明确套餐等待响应确认通知，避免共享队列重复堵塞。公共任务接口隐藏通知 token，展示等待原因。

验证：Java 67 项测试通过，其中新增6项覆盖100任务企业与另外9企业轮转、停用恢复、8个并发调度器、运行并发、通知过期/迟到确认、全局预留上限与已扣额重试。Python 41通过/1跳过，新增3项验证明确额度等待确认与未知认证失败不确认。Ruff与前端构建通过。

真实本机 MySQL V13、MinIO、RabbitMQ、Python消费者：A企业5个任务因套餐暂停保持QUEUED且attempts=0；B企业任务完成；恢复A企业后5个任务均DONE且attempts=1。所有数据为明确标记的合成文本，没有模型调用。首次验收脚本发现B企业没有知识库，补建合成知识库后重跑通过，未把第一次失败计作通过。

限制：这是调度与租户隔离验收，不是V1-45性能压力验收。Linux/OCR文件、页数、像素、解压、超时、内存与CPU实测现已补齐，见 [V1-13验收](v1-13-ocr.md)，V1-44完成。算法、故障语义、升级要求见 [调度说明](../task-scheduling.md)。
