# V1-46 备份、恢复与升级验收

2026-09-11完成以下单机Compose冷恢复分项及RabbitMQ节点身份修复后的补充验收。旧工具未保留节点身份，曾可能启动成空队列；本次重新捕获完整加密备份、恢复到新卷，并使用工具生成的覆盖配置保留原节点和持久队列。备份涵盖MySQL、原文件、RabbitMQ、Milvus、etcd及私密配置；数据库保存冻结模型身份，外部模型服务和权重须另行保留。

实际证据：

- [修正后的整套恢复](v1-46-broker-recovery-real.json)：新备份认证保存节点身份，生成恢复覆盖配置；原careflow.processing持久队列存在，原节点名一致。全部应用启动后再次核对原文件SHA256、索引重建、真实搜索/DeepSeek完成、删除文档404和旧凭证401。另在独立副本写入合成持久消息，跨4.1.4→4.2.9→4.3.5保持节点名并逐次读回。新工具对缺失节点身份的旧清单在创建卷前拒绝；不改写旧已签名备份。

- [加密存储恢复](v1-46-cold-storage-real.json)：先认证完整清单和密文，再恢复到新卷，入口保持关闭。
- [备份后事务日志重放](v1-46-binlog-replay-real.json)：公共API删除、ACL收窄和凭证撤销后，导出连续binlog并在无网络数据库重放；旧凭证401、删除原文404、旧答案隐藏。失败或中断不允许重复套用同一增量。
- [完整冷恢复](v1-46-full-cold-recovery-real.json)：独立数据库先核对最新安全状态；原文SHA256一致，索引清单在重建前后均一致，新索引代次切换，真实检索及DeepSeek SSE完成。升级后的应用镜像运行正常。另在V35历史结构中保留合成租户记录，启动当前版本执行V36迁移，原revision和用量保持不变。

计划停机RPO为0：源服务在快照前已停止并一直保持停止。历史约114秒记录未核对RabbitMQ节点，不能当作修正后的完整RTO。新记录从存储恢复开始到队列、授权、文件、索引及模型验证结束计时，包含中途执行独立性能实验的操作暂停，具体秒数见JSON；不是连续自动恢复速度或SLA。恢复使用1份保留文件和既有删除用例，不代表生产规模。

冷备份保留工具默认至少30天，按基础备份和增量依赖成组淘汰，保留每个来源最新基础备份；近期增量保护旧基础。真实加密校验、篡改拒绝、重放门禁和保留期行为均有测试。

边界：不提供在线跨存储快照、持续异地binlog归档或通用对象增量同步。备份后若新增原文件，仅重放数据库日志不够，必须重新取得一致的完整快照或另行核对文件；无法确认最新删除/撤权或文件一致性时不得开放入口。操作与失败恢复流程见[恢复手册](../recovery.md)。未发布或推送。
