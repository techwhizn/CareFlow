# V1-50 本地Git历史敏感信息扫描

2026-09-11，Gitleaks 8.30.1对本地可达的全部69个提交执行扫描，约3.47MB。使用完整脱敏输出，原始报告保存在未跟踪的`.local`中。

初次报告12处通用API Key规则命中，经逐项读取对应历史提交核对：8处是120题合成资料目录中的`cf-型号-主题`文档标识；4处是ASGI测试通过monkeypatch设置的固定内部令牌占位文本，测试没有连接外部服务。均非部署凭证。

`.gitleaksignore`仅记录这12处精确的提交/路径/规则/行号指纹，没有忽略整个测试目录或放宽通用密钥规则。后续提交的新增命中仍会失败，需要单独核对。加入精确排除后重新扫描，无未处理命中。

CI新增完整历史检出、固定版本Gitleaks与脱敏扫描。此处证明本地已获取历史的扫描结果，不证明未获取的远端引用、未跟踪环境文件或供应商侧凭证状态；也不将规则扫描称为不存在任何敏感信息的保证。远端工作流与分支保护仍未启用或验收。
